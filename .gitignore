# Build artifacts
/dist/

# Test/demo fixture data — generated or captured from real hosts, never committed
/data/

# OS cruft
.DS_Store

# Go
*.test
*.out

# Local agent tooling (Claude Code settings/worktrees, the impeccable design skill and
# its critique artifacts) -- personal to a working copy, never repo content.
.claude/
.impeccable/
.github/hooks/
.github/skills/
PRODUCT.md
